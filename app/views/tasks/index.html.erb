<div class="container">
  <div class="text-danger">My Tasks List</div>
  <ul>
    <%= @tasks.each do |task| %>
    <li><%= link_to task.name task.details, task_path(task) %></li>
    <%= end %>
  </ul>
</div>
